server:
  port: ${SERVER_PORT:9100}
  servlet:
    context-path: /
spring:
  application:
    name: reporting-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:erp}}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      maximum-pool-size: 4
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: reporting
  flyway:
    enabled: true
    schemas: reporting
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info

reports:
  storageDir: ${REPORTS_STORAGE_DIR:/data/reports}
  maxExportRows: ${REPORTS_MAX_EXPORT_ROWS:1000000}
  exportRetentionDays: ${EXPORT_RETENTION_DAYS:30}

app:
  jwt:
    secret: ${APP_JWT_SECRET}
    access-ttl: 3600
